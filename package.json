{
  "name": "expomela",
  "version": "1.0.0",
  "private": true,
  "description": "Expo Mela (expomela.com) - Exhibition & Stall Booking Platform (React + Turso/libSQL)",
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "seed": "npm --prefix server run seed",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "dev": "concurrently -n server,client -c blue,magenta \"npm run dev:server\" \"npm run dev:client\"",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start",
    "render-build": "npm run install:all && npm --prefix client run build && npm --prefix server run seed",
    "setup": "npm run install:all && npm run seed"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
